/**
 * Placeholder export for the monorepo bootstrap.
 *
 * The real surface — Event / Template / Mapping / Receipt contracts and the
 * deterministic receipt assembler — lands here. For now this constant exists so
 * downstream packages have something concrete to import against.
 */
export const CORE_PLACEHOLDER = "trace-core-ready" as const;
