/**
 * Placeholder export for the monorepo bootstrap.
 *
 * The real surface — a one-line `trace()` recorder over the x402 v2 lifecycle
 * hooks — lands here. The import below exists to wire this package to
 * `@usehaia/trace-core` end-to-end while there is no product code yet.
 */
import { CORE_PLACEHOLDER } from "@usehaia/trace-core";

export const X402_PLACEHOLDER = `${CORE_PLACEHOLDER}/x402` as const;
