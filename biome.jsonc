{
  "$schema": "https://biomejs.dev/schemas/2.5.6/schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  // The Mintlify docs site under docs/ is a separate project with its own
  // toolchain (MDX, assets, docs.json) — Biome lints the TS monorepo, not it.
  "files": { "ignoreUnknown": true, "includes": ["**", "!docs"] },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 80
  },
  "assist": {
    "enabled": true,
    "actions": { "source": { "organizeImports": "on" } }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "style": {
        // Non-null assertions are deliberate here, paired with
        // noUncheckedIndexedAccess (tsconfig.base.json); the suggested `?.`
        // rewrite changes runtime semantics rather than asserting.
        "noNonNullAssertion": "off"
      }
    }
  },
  "javascript": {
    "formatter": { "quoteStyle": "double", "semicolons": "always" }
  }
}
